version: "3.9"

services:
  python:
    image: randoom-users:latest
    container_name: get_random_users
    hostname: python
    command: ["sh", "-c", "python Get_Users.py; exec sh"]
    volumes:
      - python_data:/tmp
volumes:
  # Volume to persist data generated by the Python service in /tmp
  python_data:
